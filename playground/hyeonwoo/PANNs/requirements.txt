# ============================================================
# PANNs CNN6 × ESC-50 Inference Pipeline — Requirements
# ============================================================

# https://pytorch.org/get-started/locally/
# pytorch 자기 컴퓨터에 맞는 cuda 버전에 맞게 설치할 것

# librosa — mel-spectrogram, 오디오 로드, 리샘플링
librosa>=0.9.0,<1.0.0

# soundfile — WAV/FLAC 파일 읽기/쓰기 (libsndfile 래퍼)
soundfile>=0.12.0

# audioread — librosa 백엔드 (MP3 등 비WAV 포맷 지원)
audioread>=3.0.0


# ────────────────────────────────────────────
# 수치 연산 / 데이터 처리
# ────────────────────────────────────────────

# numpy — 텐서 변환, mel-spectrogram 배열 처리
numpy>=1.23.0,<3.0.0

# scipy — librosa 내부 신호처리 (필터, FFT 등)
scipy>=1.9.0,<2.0.0

# pandas — ESC-50 메타데이터 CSV 로드, 결과 저장
pandas>=1.5.0,<4.0.0


# ────────────────────────────────────────────
# librosa 의존 라이브러리
# ────────────────────────────────────────────

# numba — librosa 가속 (JIT 컴파일), Jetson에서 주의 필요
numba>=0.56.0

# llvmlite — numba 백엔드 (numba와 버전 쌍 필수)
llvmlite>=0.39.0

# pooch — librosa 데이터셋/모델 캐시 다운로드
pooch>=1.6.0

# lazy_loader — librosa 지연 임포트
lazy_loader>=0.2

# decorator — librosa 유틸리티
decorator>=5.0.0

# msgpack — librosa 캐시 직렬화
msgpack>=1.0.0

# joblib — 병렬 처리 (librosa, scikit-learn)
joblib>=1.2.0


# ────────────────────────────────────────────
# 네트워크 / 다운로드
# ────────────────────────────────────────────

# requests — ESC-50, PANNs 가중치, AudioSet 레이블 다운로드
requests>=2.28.0


# ────────────────────────────────────────────
# 유틸리티
# ────────────────────────────────────────────

# tqdm — 다운로드 및 배치 추론 진행률 표시
tqdm>=4.64.0
